# Material Icons Usage

This codebase refers to Google's Material Icons using `<span className="material-icons">icon_name</span>`.  
For icons to display correctly in the UI, include the following in your public/index.html `<head>`:

```html
<link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
```
